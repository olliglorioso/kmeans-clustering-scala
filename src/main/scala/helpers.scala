import math.random

package helpers:
    class CreateData (var range: Double, var points_per_cluster: Double):
        def rndm_p_from_p(point: Array[Double]): Array[Double] = {
            var randomVal = random * range
            println(randomVal)
            return Array(1.0, 2.0)
        }
    end CreateData